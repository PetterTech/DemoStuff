# Future home for Crossplane platform abstractions
#
# This folder will contain:
# - CompositeResourceDefinitions (XRDs) — define the API for landing zones
# - Compositions — map claims to managed Azure resources
# - Claims (examples) — sample requests that provision landing zones
#
# The providers in ../providers/ must be healthy before applying anything here.
